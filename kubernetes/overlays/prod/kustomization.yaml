apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: crm-prod

resources:
  - namespace.yaml
  - ../../base
  - ../../platform/mysql
  - hpa

labels:
  - pairs:
      environment: prod
