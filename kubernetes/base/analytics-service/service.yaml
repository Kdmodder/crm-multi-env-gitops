apiVersion: v1
kind: Service

metadata:
  name: analytics-service
  annotations:
    argocd.argoproj.io/sync-wave: "3"

spec:
  selector:
    app: analytics-service

  ports:
    - name: http
      port: 8086
      targetPort: 8086
      protocol: TCP

  type: ClusterIP
