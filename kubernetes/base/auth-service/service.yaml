apiVersion: v1
kind: Service

metadata:
  name: auth-service
  annotations:
    argocd.argoproj.io/sync-wave: "2"

spec:
  selector:
    app: auth-service

  ports:
  - name: http
    port: 8081
    targetPort: 8081

  type: ClusterIP
