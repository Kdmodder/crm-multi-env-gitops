apiVersion: apps/v1
kind: Deployment

metadata:
  name: notification-service
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  labels:
    version: v1
    app: notification-service

spec:
  replicas: 1

  selector:
    matchLabels:
      app: notification-service

  template:
    metadata:
      labels:
        version: v1
        app: notification-service

    spec:
      containers:
        - name: notification-service
          image: kdmodder/notification-service:1.0.0
          imagePullPolicy: Always

          ports:
            - containerPort: 8089

          resources:
            requests:
              cpu: "25m"
              memory: "64Mi"

            limits:
              cpu: "250m"
              memory: "512Mi"

          