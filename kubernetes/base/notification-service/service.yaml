apiVersion: v1
kind: Service

metadata:
  name: notification-service
  annotations:
    argocd.argoproj.io/sync-wave: "3"

spec:
  selector:
    app: notification-service

  ports:
    - name: http
      port: 8089
      targetPort: 8089
      protocol: TCP

  type: ClusterIP
