apiVersion: v1
kind: Service

metadata:
  name: integration-service
  annotations:
    argocd.argoproj.io/sync-wave: "3"

spec:
  selector:
    app: integration-service

  ports:
    - name: http
      port: 8088
      targetPort: 8088
      protocol: TCP

  type: ClusterIP
