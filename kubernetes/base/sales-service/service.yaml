apiVersion: v1
kind: Service

metadata:
  name: sales-service

spec:
  selector:
    app: sales-service

  ports:
    - name: http
      port: 8083
      targetPort: 8083
      protocol: TCP

  type: ClusterIP
