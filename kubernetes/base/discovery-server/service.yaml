apiVersion: v1
kind: Service
metadata:
  name: discovery-server
  annotations:
    argocd.argoproj.io/sync-wave: "1"

spec:
  selector:
    app: discovery-server

  ports:
  - name: http
    port: 8761
    targetPort: 8761

  type: ClusterIP
