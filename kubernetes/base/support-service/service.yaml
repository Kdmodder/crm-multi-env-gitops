apiVersion: v1
kind: Service

metadata:
  name: support-service
  annotations:
    argocd.argoproj.io/sync-wave: "3"

spec:
  selector:
    app: support-service

  ports:
    - name: http
      port: 8085
      targetPort: 8085
      protocol: TCP

  type: ClusterIP
