apiVersion: apps/v1
kind: Deployment

metadata:
  name: support-service
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  labels:
    version: v1
    app: support-service

spec:
  replicas: 1

  selector:
    matchLabels:
      app: support-service

  template:
    metadata:
      labels:
        version: v1
        app: support-service

    spec:
      containers:
        - name: support-service
          image: kdmodder/support-service:1.0.0
          imagePullPolicy: Always

          ports:
            - containerPort: 8085

          resources:
            requests:
              cpu: "25m"
              memory: "64Mi"

            limits:
              cpu: "250m"
              memory: "512Mi"

         