apiVersion: v1
kind: Service

metadata:
  name: marketing-service
  annotations:
    argocd.argoproj.io/sync-wave: "3"

spec:
  selector:
    app: marketing-service

  ports:
    - name: http
      port: 8084
      targetPort: 8084
      protocol: TCP

  type: ClusterIP
